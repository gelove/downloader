{
  "$schema": "../gen/schemas/macos-schema.json",
  "identifier": "macos",
  "description": "Capability for macOS",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": [
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "ffmpeg",
          "cmd": "$RESOURCE/bin/macos/ffmpeg",
          "args": true
        }
      ]
    },
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "ffmpeg",
          "cmd": "$RESOURCE/bin/macos/ffmpeg",
          "args": true
        }
      ]
    }
  ]
}
